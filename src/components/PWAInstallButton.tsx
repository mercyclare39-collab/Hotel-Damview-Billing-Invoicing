import React from 'react';

interface PWAInstallButtonProps {
  variant?: 'compact' | 'full' | 'sidebar' | 'header' | 'floating';
}

export const PWAInstallButton: React.FC<PWAInstallButtonProps> = () => {
  return null;
};

